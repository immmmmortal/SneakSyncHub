cluster.name: "docker-cluster"
network.host: 0.0.0.0
http.port: 9200
discovery.type: single-node
node.store.allow_mmap: false  # Optional for Docker
xpack.security.enabled: false  # Optional, to disable security features for simplicity

